# Blinking led controlled by a NodeMCU

### TL;DR

* Hook up your LED to `D5`.
* Run a couple of commands:
```bash
$ pio run -e nodemcuv2 -t upload && pio serialports monitor -b 115200
```

Weee :-)


# Contribute
Please contribute with pull-requests.